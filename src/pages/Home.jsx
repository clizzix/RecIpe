import React from 'react';
import SearchIngredients from '../components/SearchIngredients';

const Home = () => {
    return (
        <div>
            <SearchIngredients />
        </div>
    );
};

export default Home;
